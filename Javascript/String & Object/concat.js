
//  add first name and last name .

const first = "MD";
const last = "Sazzad" ;
console.log(first + " " + last);
// the same thing we can do using "concat()" :
const fullname = first.concat(' ') + last.concat()
console.log(fullname)