// Author:- SH_Hridoy
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int tc;
    cin >> tc;
    while (tc--)
    {
        string s, x;
        cin >> s >> x;
        // cout << s << " " << x<<endl;
        int len = s.size();
        for(int i=0; i<len ; i++){
            // if(s[i]==x[i] && s[i+1]==x[i+2] && s[i+2]==)
        }
    }

    return 0;
}